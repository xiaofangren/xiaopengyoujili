import { SDKAdapterInterface } from '@cloudbase/adapter-interface';
import { ICloudbaseConfig } from '@cloudbase/types';
export { NodeRequest } from './request';
export { parseQueryString, createWebStreamFromNodeReadableStream } from './utils';
export type { ICreateTicketOpts, IGetUserInfoResult, IGetEndUserInfoResult, IUserInfoQuery, IContextParam, ICompleteCloudbaseContext, } from './types';
declare function isMatch(): boolean;
declare function genAdapter(options: any): SDKAdapterInterface & {
    getSecretInfo: (config?: ICloudbaseConfig) => {
        env: string;
        secretId: string;
        secretKey: string;
        sessionToken: string;
        accessKey: string;
        secretType: string;
    };
    nodeTool: (app: any, config: ICloudbaseConfig) => void;
};
declare const adapter: {
    genAdapter: typeof genAdapter;
    isMatch: typeof isMatch;
    runtime: string;
};
export default adapter;
